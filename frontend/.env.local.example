# Local stack environment variables

# PostgreSQL connection string (local)
DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/mrfit

# NextAuth — generate secret with: openssl rand -base64 32
NEXTAUTH_SECRET=replace_me_with_openssl_rand_base64_32
NEXTAUTH_URL=http://localhost:3000

# FastAPI AI Coach URL
NEXT_PUBLIC_AI_COACH_API_URL=http://localhost:8000
